.checkins
  - if checkins.empty?
    None
  - else
    %table.checkins
      %tr
        %th Number
        %th Team
        %th= title
        %th
        %th
      - checkins.each do |checkin|
        %tr
          %td= checkin.team.number
          %td= checkin.team.name
          %td= checkin.time_left
          - if checkin.checkout.nil?
            %td.actions= link_to 'checkout', checkout_path(checkin) 
          -else
            %td.actions
          %td.actions= link_to 'delete', team_checkin_path(checkin), :confirm => 'Are you sure?', :method => :delete
