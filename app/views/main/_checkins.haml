.checkins
  - if checkins.empty?
    None
  - else
    %table.checkins
      %tr
        %th Number
        %th Team
        %th= title
        %th
        %th
      - checkins.each do |checkin|
        %tr
          %td= checkin.team.number
          %td= checkin.team.name
          %td= checkin.time_left
          - if checkin.checkout.nil?
            %td.actions= link_to 'checkout', checkout_path(checkin), :confirm => "If you're checking out a team early, make sure you know what you're doing... seriously. Think about this carefully."
          -else
          %td.actions= link_to 'delete', team_checkin_path(checkin), :confirm => 'CAREFUL.  You should only use this if you checked in the wrong team!  There is no turning back and you can royally fuck over a team.', :method => :delete
